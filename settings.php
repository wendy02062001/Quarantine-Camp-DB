<?php
    // This is where all the universal settings is stored
    $MYSQL_SERVERNAME = "localhost";
    $MYSQL_DBNAME = "quarantine_camp" ;

    $ABS_ROOT_PATH = "http://localhost/web.db";

    //available pages
    $PAGES = ["home", "login", "logout"];

    //the pages that will be displayed on the left hand side of navbar
    $NAV_PAGES = ["home"];

    //operations supported by this app
    $QUERY_OPTIONS = ["search", "insert"];
?>